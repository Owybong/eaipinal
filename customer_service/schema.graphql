type Product {
  id: Int!
  name: String!
  sku: String!
  category: String!
  price: Float!
}

type Customer {
  id: Int!
  name: String!
  email: String!
}

type Query {
  getProducts: [Product!]!
  getCustomer(id: Int!): Customer
}